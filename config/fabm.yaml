instances:
  light:
    model: gotm/light
    parameters:
      g2         : 23.0     # e-folding depth of visible fraction  (m)                     default=23.0
      g1         : 0.35
      A          : 0.58

  mod1:
    model: awi/recom
